apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - https://github.com/argoproj/argo-cd/manifests/cluster-install?ref=stable

patches:
  - path: patch-argocd-repo-server-copyutil.yaml
    target:
      group: apps
      version: v1
      kind: Deployment
      name: argocd-repo-server
